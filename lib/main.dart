// Step 1: Need to import helper library from flutter to get content on the screen
import 'package:flutter/material.dart'; // syntax to import 3rd party
import 'src/app.dart';
// Step 2: Define 'main' function to run when our app starts

void main() {
  // Create a new text widget to display text on the screen
  // Take that widget and get it on the screen
  runApp(App());
}
